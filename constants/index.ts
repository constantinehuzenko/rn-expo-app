const TEST_NUMBER = 3;

export const FOLDERS_STORAGE_KEY = "folders" + TEST_NUMBER;
export const CURRENT_FOLDER_ID = "currentFolderId" + TEST_NUMBER;
export const CURRENT_WORD_ID = "currentWordId" + TEST_NUMBER;
